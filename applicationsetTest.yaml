apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: testapp #will be overitting by jenkins 
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  generators:
    - list:
        elements:
          - path: application1
          - path: application2
  template:
    metadata:
      name: '{{path}}-testapp'
    spec:
      project: test
      source:
        repoURL: 'https://github.com/karami4yasser/gitopsDemo.git'
        targetRevision: NONE #will be overitting by jenkins   
        path: '{{path}}'
      destination:
        name: test 
        namespace: NONE #will be overitting by jenkins   
        server: ''
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
        automated:
          prune: true
          selfHeal: false