pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "Building on branch ${env.GIT_BRANCH}"'
                // Your build steps here
            }
        }
    }
}
